<Window x:Class="MyEntityFramework.AdminFilter"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MyEntityFramework"
        mc:Ignorable="d"
        Title="AdminFilter" Height="300" Width="401.667">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="87"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="166*"/>
            <ColumnDefinition Width="139*"/>
            <ColumnDefinition Width="89*"/>
        </Grid.ColumnDefinitions>
        <StackPanel>
            <Label Content="Назва Фільтру" Margin="2"/>
            <TextBox x:Name="FilterName" TextWrapping="Wrap" Text="FilterName" Height="25"/>
            <Button Content="AddFilterName" Click="Button_Click"/>
        </StackPanel>
        <StackPanel Grid.Row="1">
            <Label Content="Значення для Фільтрів"/>
            <TextBox x:Name="FilerValueName" Height="28" HorizontalAlignment="Right" Width="166"/>
            <Button x:Name="AddValue" Content="AddValue" Click="AddValue_Click"></Button>
        </StackPanel>

    </Grid>
</Window>
